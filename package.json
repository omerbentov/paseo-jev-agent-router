{
  "name": "paseo-jev-agent-router",
  "private": true,
  "version": "0.1.0",
  "description": "Paseo plugin: /route <task> picks one of your agent profiles with TypeSafe's Jev and starts the agent.",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/*.test.ts"
  },
  "devDependencies": {
    "@getpaseo/plugin": "0.8.0",
    "@tanstack/react-query": "^5.90.11",
    "@types/node": "^22.0.0",
    "@types/react": "~19.2.0",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  }
}
